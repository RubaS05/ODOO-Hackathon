server.port=8082

spring.application.name=odoo-cafe-pos

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/ododcafe}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:haridharani098}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:prathaban009@gmail.com}
spring.mail.password=${MAIL_PASSWORD:deoi tmmw rysw ynbe}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.jwt.secret=${JWT_SECRET:change-this-to-a-long-random-secret-key-min-256-bits-please}
app.jwt.expiration-ms=86400000

razorpay.key-id=${RAZORPAY_KEY_ID:YOUR_RAZORPAY_KEY_ID}
razorpay.key-secret=${RAZORPAY_KEY_SECRET:YOUR_RAZORPAY_KEY_SECRET}
razorpay.webhook-secret=${RAZORPAY_WEBHOOK_SECRET:YOUR_RAZORPAY_WEBHOOK_SECRET}

cafe.domain=${CAFE_DOMAIN:http://localhost:8080}
cafe.upi-id=${CAFE_UPI_ID:cafe@ybl}
